{
  "name": "obsidian-vault-crypt",
  "version": "1.0.0",
  "private": true,
  "description": "Archive, encrypt, version, sync, and restore an Obsidian vault.",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "build:test": "node -e \"require('node:fs').rmSync('.test-dist', { recursive: true, force: true })\" && tsc --project tsconfig.test.json",
    "test": "npm run build && npm run build:test && node tests/integration.js && node tests/user-interaction.js && node tests/plugin-bootstrap.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
